---
name: coder
description: "Spawn the `coder` agent with `$ARGUMENTS`. It executes only the"
---

# Coder

Spawn the `coder` agent with `$ARGUMENTS`. It executes only the
`coder` skill, returns its requested output and stops.

