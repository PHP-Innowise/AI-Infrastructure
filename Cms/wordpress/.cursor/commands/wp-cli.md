---
name: wp-cli
description: "Spawn `wp-cli` with `$ARGUMENTS`. It executes only the `wp-cli` skill and"
---

# WP-CLI

Spawn `wp-cli` with `$ARGUMENTS`. It executes only the `wp-cli` skill and
stops.
