---
name: block-development
description: "Spawn `block-development` with `$ARGUMENTS`. It executes only the"
---

# Block Development

Spawn `block-development` with `$ARGUMENTS`. It executes only the
`block-development` skill and stops.
