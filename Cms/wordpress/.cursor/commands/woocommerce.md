---
name: woocommerce
description: "Spawn `woocommerce` with `$ARGUMENTS`. It executes only the `woocommerce`"
---

# WooCommerce

Spawn `woocommerce` with `$ARGUMENTS`. It executes only the `woocommerce`
skill and stops.
