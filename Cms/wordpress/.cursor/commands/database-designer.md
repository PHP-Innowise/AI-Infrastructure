---
name: database-designer
description: "Spawn the `database-designer` agent with `$ARGUMENTS`. It executes only the"
---

# Database Designer

Spawn the `database-designer` agent with `$ARGUMENTS`. It executes only the
`database-designer` skill, returns its requested output and stops.

