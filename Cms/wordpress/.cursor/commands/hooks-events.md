---
name: hooks-events
description: "Spawn `hooks-events` with `$ARGUMENTS`. It executes only the `hooks-events`"
---

# Hooks and Events

Spawn `hooks-events` with `$ARGUMENTS`. It executes only the `hooks-events`
skill and stops.
