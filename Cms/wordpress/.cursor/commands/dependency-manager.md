---
name: dependency-manager
description: "Spawn the `dependency-manager` agent with `$ARGUMENTS`. It executes only the"
---

# Dependency Manager

Spawn the `dependency-manager` agent with `$ARGUMENTS`. It executes only the
`dependency-manager` skill, returns its requested output and stops.

