---
name: rest-api
description: "Spawn `rest-api` with `$ARGUMENTS`. It executes only the `rest-api` skill and"
---

# WordPress REST API

Spawn `rest-api` with `$ARGUMENTS`. It executes only the `rest-api` skill and
stops.
