---
name: multisite
description: "Spawn `multisite` with `$ARGUMENTS`. It executes only the `multisite` skill and"
---

# Multisite

Spawn `multisite` with `$ARGUMENTS`. It executes only the `multisite` skill and
stops.
