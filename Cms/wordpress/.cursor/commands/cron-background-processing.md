---
name: cron-background-processing
description: "Spawn `cron-background-processing` with `$ARGUMENTS`. It executes only that"
---

# Cron and Background Processing

Spawn `cron-background-processing` with `$ARGUMENTS`. It executes only that
skill and stops.
