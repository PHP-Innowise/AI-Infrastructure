---
spawns: api-designer-agent
phase: planning
flow-next: rest-api
flow-alternatives: [writing-plans, architecture-implementer, coder]
---

# Api Designer

Spawn the `api-designer` agent with `$ARGUMENTS`. It executes only the
`api-designer` skill, returns its requested output and stops.

