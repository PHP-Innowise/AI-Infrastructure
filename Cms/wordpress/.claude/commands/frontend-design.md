---
spawns: frontend-design-agent
phase: planning
flow-next: coder-frontend
flow-alternatives: [theme-development, block-development, writing-plans]
---

# Frontend Design

Spawn the `frontend-design` agent with `$ARGUMENTS`. It executes only the
`frontend-design` skill, returns its requested output and stops.

