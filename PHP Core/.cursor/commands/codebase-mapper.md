---
name: codebase-mapper
description: "Spawn codebase-mapper agent to map the codebase into `codebase/` documents."
---

# Codebase Mapper

Spawn codebase-mapper agent to map the codebase into `codebase/` documents.

## Input
$ARGUMENTS

## Instructions

Invoke the codebase-mapper skill and follow it completely.
