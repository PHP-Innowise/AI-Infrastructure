{% extends 'base.html.twig' %}

{% block title %}{{ mode == 'edit' ? 'Edit label' : 'New label' }} — PracticePerfect{% endblock %}

{% block body %}
    <div class="skeleton-card">
        <h1>{{ mode == 'edit' ? 'Edit label' : '+ New Label' }}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
