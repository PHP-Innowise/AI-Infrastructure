{% extends 'base.html.twig' %}

{% block title %}{{ mode == 'edit' ? 'Edit coupon' : 'New coupon' }} — PracticePerfect{% endblock %}

{% block body %}
    <div class="skeleton-card">
        <h1>{{ mode == 'edit' ? 'Edit coupon ' ~ coupon.code : '+ Create Coupon' }}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
