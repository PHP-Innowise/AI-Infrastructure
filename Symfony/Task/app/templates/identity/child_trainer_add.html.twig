{% extends 'base.html.twig' %}

{% block title %}Add a trainer for {{ child.firstName }} — PracticePerfect{% endblock %}

{% block body %}
    <div class="skeleton-card">
        <h1>Add a trainer for {{ child.firstName }}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
