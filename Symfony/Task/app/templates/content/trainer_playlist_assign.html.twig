{% extends 'base.html.twig' %}

{% block title %}Assign {{ playlist.title }} — PracticePerfect{% endblock %}

{% block body %}
    <div class="skeleton-card">
        <h1>Assign "{{ playlist.title }}" to Players</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
