{% extends 'base.html.twig' %}

{% block title %}Add Drill — PracticePerfect{% endblock %}

{% block body %}
    <div class="skeleton-card">
        <h1>Add drill to {{ playlist.title }}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
