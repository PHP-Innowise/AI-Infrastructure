{% extends 'base.html.twig' %}

{% block title %}Add a player — {{ event.title }} — PracticePerfect{% endblock %}

{% block body %}
    <div class="skeleton-card">
        <h1>Add a player</h1>
        <p class="skeleton-caption">{{ event.title }}</p>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
